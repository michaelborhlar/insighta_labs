{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py seed_profiles --file seed_profiles.json && gunicorn insighta_labs.wsgi --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/api/profiles",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
